#!/bin/bash

THEMES=()
for theme in "$ARCH_SETUP_PATH/themes/"*; do
  THEMES+=("$(basename "$theme")")
done

echo "$ARCHY_PATH/themes/"

SELECTED_THEME=$(floating-terminal gum choose "${THEMES[@]}")

set-theme "$SELECTED_THEME"
