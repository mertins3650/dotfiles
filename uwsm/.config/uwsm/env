source ~/.config/uwsm/default

export PATH="$HOME/.local/bin:$PATH"
ARCH_SETUP_PATH="$HOME/archsetup"
DOTFILES_PATH="$HOME/dotfiles"

if command -v mise &> /dev/null; then
 eval "$(mise activate zsh)"
fi
